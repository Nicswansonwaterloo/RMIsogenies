This project requires SageMath >=10.6 configured with Python 3.11. 
https://doc.sagemath.org/html/en/installation/index.html


To run the tests, make sure that sage.all is in your PYTHONPATH.
Then, from the main root RMIsogenies directory, run:
```
python -m tests.test_cgl_rm
```